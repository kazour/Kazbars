# File inventory — roles

**Current as of:** 2026-08-04 (role blurbs split out of `inventory.md`, which became a generated line-count skeleton — see `scripts/gen_inventory.py`).
**Purpose:** one row per `src/kazbars/*.py` / `tests/*.py` file (plus a few non-`.py` assets worth a note) — what it's for. Line counts live in [`inventory.md`](inventory.md), generated and machine-checked. This file is hand-maintained prose; grep it by path.

## Files

| File | Role |
|---|---|
| `src/kazbars/grids_panel.py` | `GridsPanel` container, toolbar, scrollable list, anchor-based `scale_to_resolution`. Per-row card lives in `grid_editor_panel.py` |
| `src/kazbars/grid_editor_panel.py` | `GridEditorPanel` (per-row collapsible card) + module-level `_FILL_*`/`_LAYOUT_*`/`_SORT_*` option maps; X/Y bounds pulled from `game_resolution` setting; X/Y fields built via shared `ui_forms.position_entry` |
| `src/kazbars/database_editor.py` | Buff DB UI (treeview, dialogs, category management). Edits write user deltas to `userdata/database_user.json` via `DeltaStore` (never `assets/`); `save()` diffs the effective list against the floor (`get_floor`); a Source column badges provenance (Built-in/Updated/Yours); Delete hides built-ins (tombstone) or deletes user buffs. Pure data layer in `buff_database.py`/`buff_db_layers.py` |
| `src/kazbars/grid_dialogs.py` | Add/Edit/Duplicate/BuffSelector/SlotAssignment dialogs |
| `src/kazbars/build_loading.py` | `BuildLoadingScreen` — frameless build progress (animated ring, step dots) → results-summary phases. Imports the popup chrome + frame constants from `app_popups` (incl. `keep_modal_restorable`) |
| `src/kazbars/app_popups.py` | Frameless dark popup family: shared chrome (`make_popup_shell`/`draw_close_button`/`center_popup`/`keep_modal_restorable` (Win+D-survivable modals) + `WIDTH`/`BG`/`BORDER_COLOR`/`SCANLINE_STEP`), `show_welcome_popup`, `show_about_popup` (Discord/GitHub links + animated mini-grid), `show_close_game_required_dialog` |
| `src/kazbars/buff_display_editor.py` | Default Buff Bars dialog (UI). Pure XML helpers in `buff_xml.py`; Customized-file writes go through `safe_write_text` (atomic) |
| `src/kazbars/buff_xml.py` | AoC HUD XML helpers (regex-only): `<BuffListView>` reads/writes + `read_source_color`/`set_source_color` (per-source flytext `color="0x…"`) and `read_source_direction`/`set_source_direction` (per-source `direction="1|-1|0"`, injecting the attr when the source omits it), both for the Damage Numbers color editor; `RESOURCE_LOSS_TYPES` / `INCOMING_DAMAGE_TYPES` name that editor's two macro groups. Pure — no Tk/ttkbootstrap, importable from CI without UI extra |
| `src/kazbars/buff_database.py` | `BuffDatabase` class — in-memory indexes + search, plus `load_layers()` (three-layer merge → `self.buffs` + `self.provenance`, corrupt-stock fallback to bundled `.default` in memory), `reload()`, and `current_floor()`. `load(json_path)` kept for single-file/back-compat. Pure — no Tk |
| `src/kazbars/buff_db_layers.py` | Pure three-layer buff merge: `merge_layers`/`load_effective`/`load_floor` (stock ← content ← user deltas, user wins, keyed on `ids[0]`, with `provenance`), `compute_delta` (effective → adds/overrides + tombstones), `is_valid_buff`/`_keep_valid` malformed-entry gate, `DeltaStore` (atomic `database_user.json` I/O; saves preserve entries the load filter rejected). Imports only stdlib + `safe_save_json` |
| `src/kazbars/app.py` | Entry point + `KazBarsApp` root window (widgets, menu, lifecycle); `__init__` calls `ensure_layout()`, points `profiles_path`/`settings_path` at `userdata/`, builds `self.settings = Store(PREFS_SCHEMA, userdata_root())`, loads the buff DB via `database.load_layers(stock, content, user)` (DB editor wired with a `DeltaStore` + `get_floor`), and kicks the OTA check (`content_update.check_and_apply`, deferred to first-launch completion on a fresh install); Updates menu carries the auto-update toggle + check-now/revert; Extras menu is two captioned `header` groups — "/reloadui to apply" (Default buff bars…, Damage number colors…) and "Build & Install to apply" (Damage number mod…, Stopwatch…, Inspect panel…); File menu carries "Manage profiles…" |
| `src/kazbars/__main__.py` | Process entry point — logging setup + `KazBarsApp().mainloop()`; invoked by `python -m kazbars` |
| `src/kazbars/__init__.py` | Package version + `APP_NAME` + `CONTENT_BASELINE_VERSION` (the shipped OTA content version, stamped by `gen_manifest`); `__version__` is the hatchling dynamic-version source |
| `src/kazbars/ui_widgets.py` | Leaf "core glue": `blend_alpha`, `add_tooltip` (+ `_InAppToolTip`), `app_toast`, `flash_status_bar`, `debounced_callback`, and the event-binding helpers (`bind_card_events`/`bind_button_press_effect`/`bind_label_hover_colors`/`bind_label_press_effect`). Imports nothing from `ui_headers`/`ui_forms`/`ui_collapsible` — they depend on it |
| `src/kazbars/ui_headers.py` | Dialog/app headers: `create_dialog_header`, `create_app_header`, `update_app_header_color`, `create_tip_bar`. Imports `blend_alpha` from `ui_widgets` |
| `src/kazbars/ui_forms.py` | Form fields + shared settings-panel builders: `labeled_spinbox`/`labeled_combobox`/`position_entry`, `draw_grid_cells`, `create_rounded_rect`, `ColorSwatch` (rounded swatch + themed `ColorChooserDialog`), and the group both config panels share — `create_card`, `create_status_block`, `create_slider_row` (optional `on_commit` — pass `None` on a staged form whose Apply is the only write, and the release bindings are skipped; optional `value_width` for the readout label, `notch` for a centered default tick on symmetric sliders, `label_width` to align descriptor columns, and `label_sink` so a master gate can grey the row's descriptor + value labels alongside its control), `toggle_button_state`, `create_toggle_action_button`, `refresh_toggle_button`. Imports `add_tooltip` from `ui_widgets` |
| `src/kazbars/ui_collapsible.py` | `CollapsibleSection` (with `set_dimmed`). Imports `blend_alpha` from `ui_widgets` |
| `src/kazbars/live_tracker_panel.py` | Live Tracker Toplevel orchestrator |
| `src/kazbars/timer_overlay.py` | In-game transparent Live Tracker overlay — a `HudOverlay` consumer (`_render_content`: two text rows + cycle-timer dock with 8-direction stroke; `_measure`: font-derived auto-size, no resize handle) |
| `src/kazbars/ui_components.py` | Toast stack — pure `ToastModel` (cap 3 + queue, two-tier priority, coalesce-by-key) + `ToastManager` Tk renderer (single `_layout()` authority, hover-pause, click-dismiss) — plus `DragReorderManager`, scrollable frame |
| `src/kazbars/grids_generator.py` | AS2 code generation from grid configs (optional console hooks via `include_console`, whose `{{CONSOLE_*_PERSIST}}` tokens also carry the console's `loadState`/`saveState` (ordered before the pinned `createConsole()`); optional cast-timer overlay hooks + `d.CAST` block via `cast_config` → `include_cast_timer`; optional stopwatch hooks + `d.SW` block (x/y, `fontSize`, collapsed) via `stopwatch_config` → `include_stopwatch`; optional inspect-panel hooks + `d.INS` block via `inspect_config` → `include_inspect`). Also holds `CUSTOM_ICON_LINKAGE` (null-icon buff IDs → baked `base.swf` symbol names), emitted into `KazBarsData.CUSTOMICON` |
| `src/kazbars/boss_timer.py` | Boss timer state + UI |
| `src/kazbars/instructions_panel.py` | Help/instructions view — two-pane knowledge base: categorized nav + live search over a `SECTIONS` data model, content rendered per selection |
| `src/kazbars/first_launch.py` | First-launch dialog + post-dialog orchestrator (`run_first_launch`); fires the deferred OTA content check on completion |
| `src/kazbars/custom_menu_bar.py` | Canvas-based dark menu bar (active-cascade phosphor underline; ttkb-safe Canvas spacers; supports `command`, `separator`, `checkbutton` and `header` entries — a `header` is a muted `FONT_SMALL` group caption, never added to `_rows`, so hover and keyboard nav skip it; keyboard access via F10 + arrows/Return/Escape — root key bindings installed once and gated, never unbound) |
| `src/kazbars/combat_monitor.py` | Combat log parser feeding the tracker (worker loop carries a thread-identity guard so a quick Stop→Start can't leave two workers double-firing) |
| `src/kazbars/cast_timer_panel.py` | Cast Timer dialog (Extras ▸ Cast timer…) — master build-gate toggle, the two baked positions (Player/Target X/Y), and a Text section (Bold, font size 8–48, Show = Elapsed/Total/Both, `ColorSwatch`) with a live Arial sample preview canvas beside it (`_redraw_preview` shrinks the size to fit the square). Master drives both sides together (`enableP == enableT == enabled`); single-instance via `app.cast_timer_dialog`; withdraw → build → `restore_window_position(offset=(60, 60))` → `bind_window_position_save(save_size=False)` → deiconify, `<Escape>` closes, dark titlebar re-asserted on `<Map>`; persists the `cast_timer` prefs dict. Rose header accent |
| `src/kazbars/build_executor.py` | MTASC compile + deploy; Damage Numbers backup/restore (bundled pristine as the stock source of truth, install via stage-to-temp then a single `os.replace` commit). Never touches the skin's TextColors.xml — colors *and* directions belong to the Damage Number Colors panel, so they survive build, mod-off and uninstall alike |
| `src/kazbars/profile_io.py` | Profile load (read+apply split, with auto-anchor-scale on resolution mismatch) / save (build+write+commit, `silent=` for piggyback saves) / new / open + missing-buff warning. Persists the integer `profile_schema` (migration ladder, empty now) alongside `grids`; the cast timer left the payload when it moved to prefs — no migrator, so an old profile's `cast_timer` block is never read and drops out on the next save. `resolve_default_profile_path` (user `default_profile` → OTA `content/Default.json` → stock); both shipped defaults force Save As; `set_default_profile` writes the prefs pointer |
| `src/kazbars/profile_share.py` | Pure `KZBARS1:` codec — `encode_profile`/`decode_profile` (gzip+base64), `collect_referenced_user_buffs` (the user-provenance buffs a profile references, across int-ID + name forms) + `merge_imported_buffs` (merge into `database_user.json`: skip on an ID collision, rename on a name-only collision), so an export is self-contained. Imports stdlib + `buff_db_layers` only |
| `src/kazbars/profile_manager.py` | Profile Manager dialog (Tk) — list / load / rename / duplicate / delete / set-default over `userdata/profiles/`, plus `KZBARS1:` export-to-clipboard + import (one confirmation → write + `merge_imported_buffs` → re-merge DB → one toast). Pure codec in `profile_share` |
| `src/kazbars/game_folder.py` | Game folder UI + Aoc.exe bypass (with install/remove reconciler) + uninstall |
| `src/kazbars/game_resolution.py` | Game resolution dialog + anchor-rescale all loaded grids on apply |
| `src/kazbars/settings_backup.py` | Backup & Restore dialog + pure zip layer (`write_backup_zip`/`read_manifest`/`restore_zip`, `funcom_prefs_path`, `_funcom_summary`) — bundles `%LOCALAPPDATA%\Funcom\Conan\Prefs` + the `userdata/` allowlist (`profiles/`, `settings/`, `database_user.json`, `prefs.json`) into one zip; the OTA `content/` cache is never a parameter so it can't leak. Restore snapshots first (outside `userdata/`), guards zip-slip, resyncs prefs; machine-local `prefs.json` is restored only when the dialog checkbox opts in. Isolated satellite, no cross-imports |
| `tests/test_buff_xml.py` | Round-trip smoke test for the `buff_xml` helpers: `<BuffListView>` attrs + `read_source_color`/`set_source_color` (0x form, idempotent, missing-element, direction-preserving) + `read_source_direction`/`set_source_direction` (named element only, all three values, injection when the attr is missing, idempotent, byte-preserving) + the two direction group lists |
| `src/kazbars/build_action.py` | Build & Install flow (compile + install on a worker thread) |
| `src/kazbars/ui_helpers.py` | Design tokens + `setup_custom_styles` + `style_treeview_heading` |
| `src/kazbars/live_tracker_settings.py` | Tracker persistence — `TIMERS_DEFAULTS`/`TIMERS_RANGES` re-expressed as a `settings_core.Schema`; validation + atomic I/O delegate to the engine. (The pre-rebrand `timers_settings.json` filename + `transparent_bg`/`opacity` key migrations were dropped — clean start, no legacy installs.) |
| `src/kazbars/grid_model.py` | Grid dataclasses, `parse_resolution`, `get_game_resolution_or_default`, anchor-based `scale_grid_position` (X center / Y bottom anchored), `dedupe_grid_ids` load-time rename of duplicate grid names |
| `tests/test_data_integrity.py` | Buff-ref resolution smoke test (assets stock pair byte-identity + `Default.json` whitelist resolution — strengthened now the app never writes assets) |
| `tests/test_buff_db_layers.py` | Three-layer merge unit gate — precedence (stock←content←user) + provenance, tombstones (incl. user re-add beating its own tombstone), missing/corrupt-layer fallbacks, `load_effective`/`load_floor`, `compute_delta` (add/override/tombstone/cosmetic-no-op/primary-ID-change), `DeltaStore` round-trip |
| `tests/test_content_update.py` | OTA channel (no network) — manifest parse/reject, `is_newer` + `min_app_version` gate, `verify_sha256`, `apply_content`/`rollback` (first-update clears, revert-to-previous, mid-swap self-heal, user deltas untouched), `summarize_changes`, and the dispatcher via `_worker` with a fake synchronous app + injected downloader (apply→re-merge→persist, not-newer skip, sha-mismatch abort, toggle-off + edit/build apply-guard defers) |
| `tests/test_manifest.py` | OTA drift guard — `ota/manifest.json` well-formed, its per-payload sha256 matches the committed stock files, and `CONTENT_BASELINE_VERSION` == the manifest's `content_version` (stamped together by `gen_manifest`) |
| `tests/test_profile_share.py` | `KZBARS1` codec — encode/decode round-trip, corrupt/truncated rejection, `collect_referenced_user_buffs` (exactly user-provenance refs across int-ID + name forms, de-duped), self-contained round-trip into an empty DB, import merge (skip on any-ID collision, rename on name collision) |
| `tests/test_profile_io.py` | `profile_io` pure pieces — `PROFILE_SCHEMA_VERSION` + the (empty) `_migrate_profile` ladder, and `resolve_default_profile_path` precedence (user `default_profile` → OTA `content/Default.json` → stock, ignoring a missing user default). Also pins `build_profile_payload` to carry no `cast_timer` key — the timer is machine-local prefs, so a profile (or a shared KZBARS1 string) can't drag one machine's coordinates onto another |
| `src/kazbars/build_utils.py` | Compiler discovery + path helpers |
| `src/kazbars/cast_timer.py` | Cast-timer overlay config (pure data): a `settings_core.Schema` of defaults/validators, `validate_config`/`get_default_config` over `validate_all`, `is_enabled` gate (master `enabled` AND a side on). Backs the machine-local `cast_timer` prefs field, the `cast_timer_panel` dialog and the build's `cast_config`. No Tk |
| `src/kazbars/stopwatch.py` | In-game stopwatch config (pure data): a `settings_core.Schema` validated via `validate_all` (`enabled`, `x`/`y`, `fontSize` 8–48, `startCollapsed`); its `enabled` flag is the `include_stopwatch` build gate, and `fontSize` drives every dimension in the stub. No Tk |
| `src/kazbars/stopwatch_panel.py` | Stopwatch dialog (Extras ▸ Stopwatch…) — build-gate toggle, baked default X/Y, baked font size (8–48, the whole panel scales with it), start-collapsed; single-instance via `app.stopwatch_dialog`; withdraw → build → `restore_window_position` → `bind_window_position_save(save_size=False)` → deiconify, `<Escape>` closes, dark titlebar re-asserted on `<Map>`; persists the `stopwatch` prefs dict |
| `src/kazbars/inspect.py` | Target inspect panel config (pure data): a `settings_core.Schema` (`enabled`/`x`/`y`/`fontSize`/`startCollapsed`) validated via `validate_all`; its `enabled` flag is the `include_inspect` build gate. No Tk |
| `src/kazbars/inspect_panel.py` | Inspect Panel dialog (Extras ▸ Inspect panel…) — build-gate toggle, baked default X/Y, baked font size (8–48, the whole panel scales with it), start-collapsed flag, the PvP/perks section toggles, and a Console section carrying the flat `build_console` build gate (one Apply, one `save()`; when the validated `inspect` dict is unchanged only the console box can have moved, so the toast says "Console saved" instead of reporting an inspect save that isn't); single-instance via `app.inspect_dialog`; withdraw → build → `restore_window_position(offset=(30, 30))` → `bind_window_position_save(save_size=False)` → deiconify, `<Escape>` closes, dark titlebar re-asserted on `<Map>`; persists the `inspect` prefs dict |
| `src/kazbars/damageinfo_settings.py` | Damage Numbers config (pure data; validation + atomic I/O delegate to `settings_core` — `GLOBAL_SETTINGS` doubles as the Schema's source): `GLOBAL_SETTINGS` bake-map (symmetric offset ranges + `invert`/`relative` UI flags for the position sliders + target file + regex), `GAME_DEFAULTS`, `PRESETS` (Default/Performance — carry the animation timing), `SPREAD_SPACING_OPTIONS` (one radio → both zig-zag offsets) + `spread_spacing_option`, the per-source color catalog (`PAIRED_GROUPS`/`SHARED_SOURCES`/`ALL_SOURCE_NAMES`) + `normalize_color` (the color editor's UI catalog — colors and directions themselves are NOT settings; `fixed_col_split` "Split signed numbers into Column B" and `other_resource_loss_to_target` "Keep enemy drains overhead" are SWF-only and never touch TextColors.xml), validate/`compute_final_value`/`readout`/`apply_preset`, `is_offset_key`, load/save. No Tk |
| `src/kazbars/damageinfo_generator.py` | Bakes setting offsets into the lean AS2 tree and MTASC-injects the pristine `DamageInfo.swf` (`build_damageinfo` via `build_utils.compile_as2`). No Tk |
| `src/kazbars/damageinfo_panel.py` | `DamageNumbersPanel` Toplevel (Extras ▸ Damage number mod…) — master enable gate, presets, then cards Behavior (all toggles, off by default) / Shadow / Rising numbers / Dropping numbers / Zig-zag numbers; offset sliders (centre-notched; vertical ones reversed) + the coupled `Spread-spacing` radio in a scrollable body, with the Column B rows hidden until the split toggle is on. **Model B** — modal (`grab_set` after the `withdraw`, as in every other dialog here), every control stages into `self.settings` and only the footer Apply calls `dis.save_settings` (then toasts + `destroy()`); Cancel / X / `<Escape>` discard. The Apply toast branches on the master gate — enabled ⇒ success "Build & Install to apply", disabled ⇒ info "next build restores stock". No number/label size slider (AoC's own Options slider covers it). The per-source color editor is its own Extras-menu entry (Damage number colors…), not a child of this panel |
| `src/kazbars/damageinfo_colors_panel.py` | `DamageNumberColorsPanel` — modal Apply/Cancel color + direction editor (Extras ▸ Damage number colors…, mirrors the Default Buff Bars dialog): a "Number directions" macro card (two checkboxes staging `RESOURCE_LOSS_TYPES` / `INCOMING_DAMAGE_TYPES`, their tick derived from the rows), then all 35 sources in a 2-column self/other card layout + a shared resources/misc card, each row label · Rising/Dropping/Zig-zag combobox · `ui_forms.ColorSwatch` · ↺ reset, so the reset sits past both things it resets; seeds from the skin's live TextColors.xml, "reset" reads the stock color *and* direction from Default/. Apply writes directly via the module's pure `apply_colors(game_path, colors, directions)` (element-scoped `color="0x…"` / `direction=` edits → Customized/, created from Default/; only moved directions are sent, since a direction write injects the attr when absent; one-time `.bak`; no build, no master-enable gate — this panel is the file's only writer). Opener validates the game folder |
| `src/kazbars/window_position.py` | Window geometry save/restore — all windows keyed under the single `window_positions` prefs dict field (`save_window_position`/`restore_window_position`/`bind_window_position_save`; `clamp_to_screen` is multi-monitor aware) |
| `src/kazbars/settings_core.py` | Schema-driven settings engine (pure, no Tk): `Field`/`Schema`/`Migration` + a stateful `Store` + the functional `coerce`/`validate_all`/`get_defaults`/`load`/`save`. One load/migrate/validate/fill/atomic-save path behind every settings file. **Strict drop-unknown** — every persisted key must be a declared `Field` or it's erased on save, so dynamic key namespaces are declared as one structured-dict `Field` with a custom `validate=`. Migration ladder ships empty (clean start), machinery live for the first post-publish bump. Backs `deeps_settings`/`live_tracker_settings`/`damageinfo_settings`/`prefs`. Imports only stdlib + `settings_manager.safe_save_json` |
| `src/kazbars/settings_manager.py` | The `get_setting`/`set_setting` module proxy + the atomic temp+rename writers `safe_write_text`/`safe_save_json` (what `settings_core`, `profile_io`, and the Customized-skin writers build on; `safe_save_json` delegates to `safe_write_text`). `SettingsManager` is retired; `init_settings` now receives a `settings_core.Store` |
| `tests/test_settings_manager.py` | `safe_write_text`/`safe_save_json` atomic-write helpers — round-trip + parent-dir creation, overwrite leaves no `.tmp` behind, JSON round-trip |
| `src/kazbars/userdata.py` | `userdata/` storage root: path resolution (`userdata_root`/`prefs_path`/`settings_dir`/`profiles_dir`/`database_user_path`/`content_dir`/`content_backup_dir`) + `ensure_layout()` — creates the tree and seeds an empty `database_user.json` + `content/` dirs on first launch. Idempotent; the whole startup-data step (no archive, no migrate). Pure, no Tk |
| `src/kazbars/prefs.py` | Machine-local prefs: `PREFS_SCHEMA` (strict `settings_core.Schema` for `prefs.json`, with the structured `window_positions` + `buff_display_section_open` + `stopwatch` + `inspect` + `cast_timer` dict fields, plus the OTA `content_version`/`auto_update_content`). `app.settings` is a `settings_core.Store` on this schema; the `get_setting`/`set_setting` proxy + ~20 `app.settings` call sites use its `get`/`set`/`save()`/`reload()`/`data` surface directly. Pure, no Tk |
| `src/kazbars/update_check.py` | GitHub release check — silent launch check + Updates-menu manual check (`check_for_updates_manual`, always answers with a toast) over the shared `fetch_latest` (also used by the About popup); named main-thread dispatchers; `_parts` keeps each dot-component's leading digits so suffixed tags (`2.3.0-rc1`) still compare |
| `tests/test_update_check.py` | `_parts` version compare — plain semver, numeric (not lexicographic) ordering, suffixed tags keep their numeric prefix (`2.3.0-rc1` → `(2, 3, 0)`, never falsely up-to-date), garbage tag → `()` conservative |
| `src/kazbars/content_update.py` | OTA reference-content channel: pure helpers (`parse_manifest`/`is_newer`/`app_supports`/`verify_sha256`/`apply_content`/`rollback`/`summarize_changes`) + a Tk dispatcher (`check_and_apply`/`revert`). Polls `ota/manifest.json`, SHA-verifies, atomically swaps `userdata/content/` with `.bak/` rollback, re-merges live, one toast; defers on edit/build/first-launch. NOT on the mypy gate (imports tkinter) |
| `src/kazbars/ui_tk_style.py` | Raw-tk widget styling + dark titlebar |
| `src/kazbars/foreground.py` | Pure ctypes foreground probe (`app_or_game_foreground`) — no Tk/PIL. Shared by both clusters + the `ForegroundWatcher`; defaults to "show" on any probe failure |
| `src/kazbars/focus_watcher.py` | `ForegroundWatcher` — one app-owned ~250 ms tick that probes foreground once and fans `set_focus_suppressed` out to every registered overlay. Replaced the per-cluster focus polls |
| `src/kazbars/paths.py` | Path constants: `PACKAGE_ROOT`/`ASSETS`/`KAZBARS_ASSETS` (bundled read-only assets, dev + frozen) + `app_path()` (user-writable runtime root next to the .exe) |
| `tests/test_imports.py` | Import-graph smoke test |
| `tests/test_docs_in_sync.py` | this file's guard (no phantom rows, every `*.py` listed, line counts within `max(40, 25%)` tolerance) + `database-changelog.md` buff-total parity + flows.md ref guard (no `file:line` refs, referenced files exist, step subject callables resolve in the referenced file's AST) + CHANGELOG release guard (every `v*` tag has a `## [X.Y.Z]` section; CI checks out full history, so it bites there and in pre-commit) |
| `tests/test_mypy_gate.py` | mypy-gate loop closer — the Tk-free module set (AST-derived) must equal `[tool.mypy] files` in both directions, no phantom entries |
| `tests/test_app_contract.py` | satellite ⇄ `KazBarsApp` attribute contract — every `app.<attr>` access in app-taking functions must be defined on `KazBarsApp` (Tk `dir()` + `self.X` assigns), with a floor canary against vacuous passes; `getattr(app, …, default)` + `self.app.X` chains deliberately out of scope |
| `tests/test_design_tokens.py` | design-token guard — no `#hex` literal outside `ui_helpers.py` (pure black/white blend anchors allowed; Live Tracker overlay palette allowlisted per-literal; stale-allowlist + detector canaries) |
| `tests/test_panels_construct.py` | Panel-construction smoke — boots a real `KazBarsApp` (tmp userdata, network no-opped, prefs pre-seeded) and opens all 10 satellite panels via their app-side openers, closing each via `WM_DELETE_WINDOW`; AST canary self-enrolls new `_open_*`/`_show_*` openers (or an explicit DEFERRED entry). Also hosts the ui_widgets binding-hygiene tests (idempotent `add_tooltip`, hover-colors add="+") — they must ride this module's Tk instance, since a second Tcl interp won't initialize on the hosted CI runner. Windows-gated |
| `tests/test_grids_generator.py` | `CodeGenerator.include_console`, `include_cast_timer` (via `cast_config`), `include_stopwatch` (via `stopwatch_config`), AND `include_inspect` (via `inspect_config`) on/off output checks |
| `tests/test_as2_class_names.py` | AS2 `KazBars*` class-name guard — generator emits `class KazBars {` / `class KazBarsData {`, and every `stubs/*.as` declares a `KazBars`-prefixed class matching its filename. Text-only (no MTASC, no `base.swf`), so it runs anywhere; a rename here breaks the `base.swf` bind silently |
| `tests/test_subprocess_flags.py` | AST guard — every `subprocess.run`/`Popen`/`call`/`check_call`/`check_output` under `src/kazbars/` passes `creationflags`, plus a floor canary. Without it a non-elevated user pays a ~5s console-allocation stall per spawn |
| `tests/test_cast_timer.py` | `cast_timer` config defaults, clamping, color/enum sanitization, `is_enabled` build gate |
| `tests/test_stopwatch.py` | `stopwatch` config defaults, X/Y and `fontSize` clamping, bad-value fallback, sanitized shape |
| `tests/test_inspect.py` | `inspect` config defaults (off by default), X/Y + fontSize clamping, start-collapsed coercion, bad-value fallback, sanitized shape |
| `tests/test_cluster_isolation.py` | Static-import guard for the Live Tracker AND Deeps clusters (no inbound except `app.py`; cluster imports stdlib + cluster + shared infrastructure only; no cross-import) |
| `tests/test_resolution_scaling.py` | Anchor-formula regression test (`scale_grid_position` predictions for 1080p → 1440p / 4K against `Default.json`) |
| `tests/test_grid_model.py` | `validate_grid` load-time sanitation — junk values fall back to defaults (no raise), numeric-string coercion, out-of-range clamps; `dedupe_grid_ids` duplicate-name renames |
| `tests/test_settings_backup.py` | `settings_backup` pure layer — backup→restore over the `userdata/` allowlist (profiles/settings/`database_user.json`/`prefs.json`), `content/` never archived, `prefs.json` restored only when opted in, `*.tmp` exclusion, manifest accept/reject, prefs locator, zip-slip guard |
| `tests/test_settings_core.py` | `settings_core` engine unit gate — Field coercion (bool/int/float/choices/custom-validate/passthrough), strict drop-unknown + fill-missing, `get_defaults` freshness, migration ladder (ordering + idempotent fixpoint + empty no-op), atomic I/O (missing/corrupt → defaults, round-trip, no leftover `.tmp`, `schema_version` stamp, structured-dict round-trip), and the `Store` facade |
| `tests/test_userdata.py` | `userdata` layout — `ensure_layout()` creates the tree + seeds an empty `database_user.json`, is idempotent (never reseeds existing data), and every named subpath resolves under `userdata/` (`app_path` monkeypatched to tmp) |
| `tests/test_prefs_schema_covers_all_proxy_keys.py` | Strict-schema safety net — greps every `get_setting`/`set_setting`/`app.settings`/`self.settings`(app.py) proxy key in the tree (resolving `UPPER_CASE` constants) and asserts each is a declared `PREFS_SCHEMA` Field, so strict validation can't silently erase a real setting |
| `tests/test_overlay_config.py` | `OverlayConfig` dataclass + per-cluster adapters (Deeps `overlay_*` keys / Live Tracker bare keys) round-trip |
| `tests/test_hud_overlay.py` | `HudOverlay` visibility gate (stubbed engine, no win32) — `resize()`/`set_locked()` repaint only while wanted-visible (no ghost overlay while stopped) yet still apply geometry/lock state |
| `tests/test_focus_watcher.py` | `ForegroundWatcher` tick + fan-out suppression with an injected probe (no display needed) |
| `tests/test_foreground.py` | `app_or_game_foreground` probe — own-process gate, AoC match, show-on-probe-failure default |
| `tests/test_timer_sizing.py` | Live Tracker overlay font-derived auto-size (`_measure`) bounds |
| `tests/test_toast_model.py` | Pure `ToastModel` core — duration defaults, coalesce (live/queued/exiting, carries `on_click`+`style`, re-sorts the queue on a priority-band crossing), cap + FIFO promotion, two-tier priority, non-overlapping slot offsets, and a `ToastManager` guard against coalescing onto a promoted-but-unmounted toast |
| `tests/test_toggle_button_state.py` | Pure `toggle_button_state` label/bootstyle flip shared by both panels' Start↔Stop toggle |
| `tests/test_log_name.py` | `sanitize_log_name` CombatLog filename trimming (`CombatLog-2026-05-16_2152` → `CombatLog_2152`) |
| `tests/test_boss_timer.py` | `BossTimer` cycle/syphon/double-seed transitions + phase state machine (time-driven, no sleeps) |
| `tests/test_combat_monitor.py` | `_process_line` dispatch, player extraction, latest-log discovery, start-without-folder guard, quick-restart stale-worker regression (thread-identity guard) |
| `tests/test_build_executor.py` | Install/uninstall orchestration (both modes), legacy cleanup, `create_scripts` markers, xml.add, launcher detect, `tasklist` argv, Damage Numbers backup-once/install/revert/uninstall + orphan-mod recovery — no MTASC/Tk |
| `tests/test_build_compile.py` | MTASC compile-integration — whole codegen → bundled `mtasc.exe` exit-0 (escaping end-to-end + console/cast/stopwatch/inspect variants + all-features-together); the only check bridging Python-side correctness to the SWF side; win32 + compiler gated |
| `src/kazbars/deeps_panel.py` | `DeepsPanel` Toplevel — status row, Start/Stop, Lock + Layout, appearance (size/background sliders, font fixed to Segoe UI), Readout card (window width + a Style preset radio — Live/Steady/Calm — bundling smoothing/round/refresh), Alarm & Tints card (DPS-out alarm slider over the 1000–4000/s band + Tank/Standard survival-tint preset radios + a live breakpoint caption), 5-cell visibility picker, pet toggle. Owns the meter + overlay + 100 ms UI tick + alarm hysteresis state machine |
| `src/kazbars/deeps_meter.py` | `DeepsMeter` daemon thread — tail loop, log rotation detection, `is_live` probe via `CreateFile` exclusive-share, configurable rolling-window width (`set_window_seconds` recreates the trackers); worker exit checks carry a thread-identity guard so a quick stop→start can't leave two workers double-counting. Publishes `MeterSnapshot` (focus is no longer probed here — the shared `ForegroundWatcher` owns it) |
| `src/kazbars/deeps_overlay.py` | Five-cell numbers display (DPS out/in, HPS out/in, ΔHP in). Two layouts (horizontal/vertical), 8-direction stroke text, 2 Hz alarm pulse on DPS-out, net-HP tints, click-through lock. `_DisplaySmoother` eases the drawn digits (EMA + coarse rounding + redraw-cadence gate); numbers use smoothed values, colors use the raw snapshot |
| `src/kazbars/overlay_engine.py` | Shared PIL + win32 overlay engine: `LayeredOverlay` (per-pixel-alpha win32 blit) + `HudOverlay` (shared backdrop / lock chrome / drag / visibility) + `OverlayConfig` (geometry+appearance dataclass) + `load_font`/`FONT_FAMILY_CHOICES`. Both the Deeps and Live Tracker overlays are thin `render_content` + `measure` consumers |
| `src/kazbars/deeps_parsers.py` | Pure parsers (no Tk, no threading). 5 entry points: `parse_outgoing_damage`, `parse_incoming_damage`, `parse_incoming_heal`, `parse_outgoing_heal`, `parse_pet_hit` (own-pet only). Damage/heal regexes byte-identical to `Deeps/rust/aoc-damage` + `aoc-heal` |
| `src/kazbars/deeps_trackers.py` | `DamageOutTracker`, `DamageInTracker`, `HealsInTracker` (3-bucket per-bucket warm-up), `HealsOutTracker` |
| `src/kazbars/deeps_settings.py` | `deeps_settings.json` defaults + ranges re-expressed as a `settings_core.Schema` (validation + atomic I/O delegate to the engine). Keeps the readout-tuning keys (`window_seconds`, `smoothing`, `round_step`, `refresh_ms`, `survival_preset`). `survival_preset` (Tank/Standard) drives the four ΔHP-in tint thresholds via `normalize_survival_preset` + the `_SURVIVAL_PRESETS` table (panel-invoked domain logic, kept out of the load path), twin of the readout-preset machinery |
| `src/kazbars/deeps_rolling_window.py` | `RollingWindow` data structure — record/prune/sum_since/count_since/first_event |
| `src/kazbars/assets/deeps/pets.json` | Pet-name registry — lifted from `Deeps/rust/aoc-damage/data/pets.json` |
| `tests/test_deeps_parsers.py` | 163 behavior-table cases from Deeps's Rust tests + the own-pet gate |
| `tests/test_deeps_meter.py` | 40 cases — file selection, lifecycle (incl. the quick-restart stale-worker regression), `_process_line` dispatch, configurable-window reset, attach-at-EOF vs boundary resume, held-open-file end-to-end |
| `tests/test_deeps_trackers.py` | 28 cases — warm-up, decay, reset, per-bucket warm-up for heals |
| `tests/test_deeps_settings.py` | 110 cases — defaults, validation (incl. readout-tuning keys + `survival_preset`), `normalize_survival_preset`, round-trip, corrupt-file fallback |
| `tests/test_deeps_rolling_window.py` | 13 cases — primitive smoke + decay-during-silence |
| `tests/test_deeps_overlay.py` | 30 cases — pure helpers + 5-cell IDs/labels + `_DisplaySmoother` (EMA/rounding/cadence) (visual behaviour is manual QA) |
| `tests/test_damageinfo_settings.py` | Damage Numbers config — defaults/schema invariants, symmetric offset ranges + common X/Y step + `is_offset_key` + `invert`/`relative` sets, offset clamping, enum/bool coercion, `compute_final_value`, `readout`, `apply_preset`, round-trip/fallback, per-source color catalog↔engine parity + `normalize_color` (colors are not persisted settings — a legacy `source_colors` key is dropped on save) |
| `tests/test_damageinfo_colors_panel.py` | Data-layer test for the colors panel: `_read_colors` (none/missing → {}, bare-upper hex, omits colorless) + `_read_directions` (missing file, omits attrless sources) + `apply_colors` (none when no TextColors anywhere, create-Customized-from-Default leaving Default stock, edit-existing-Customized + one-time `.bak`, siblings preserved, colorless source is a no-op, reset-to-Default flow; `directions=None` leaves every direction alone, all three values round-trip, the attr is injected when the source omits it, colors and directions survive each other). UI is manual-QA-only |
| `tests/test_damageinfo_generator.py` | Regex↔AS2 coupling guard (every bake pattern matches the shipped source **exactly once**) + shipped-constant == GAME_DEFAULTS (the offset-0-is-stock invariant) + bake correctness (offset→final, dual-axis shadow blur, enum/bool) + per-content-scale guard (Size survives the pop-in) + easing-ships-Quad guard + hard-fail on drifted source; no MTASC |
