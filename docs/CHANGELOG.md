# Changelog

All notable changes to KazBars will be documented in this file.

The format is based on [Keep a Changelog](https://keepachangelog.com/en/1.1.0/).

## [2.0.0] — Unreleased — KazBars rebrand

### Changed
- **Renamed Kaz Grids → KazBars.** The community has always called the mod "KazBars" (the original predecessor name); this release closes the gap. The Python package is now `kazbars` under a `src/` layout, the entry point is `python -m kazbars`, and the deployed Flash module installs as `KazBars.swf` under `Data/Gui/Default/Flash/` with `Data/Gui/Aoc/KazBars/` for the launcher-bypass XML adds. XML element IDs (`name=`, `variable=`, `config_name=`, `criteria=`) and the auto-load marker (`# KazBars auto-load`) all rebrand consistently. Predecessor filenames (`kzgrids.swf`, `KzGrids.swf`, `KazGrids.swf`, `Aoc/KzGrids/`, `Aoc/KazGrids/`, `Aoc/Kazbars/`, `# KzGrids auto-load`) are auto-cleaned by `cleanup_legacy_files` on every install, so existing users get a clean takeover with no manual uninstall step.
- **AS2 internals frozen.** The precompiled `base.swf` hardcodes `m_Module = new KzGrids(this)`, so the AS2 class names (`KzGrids`, `KzGridsData`, `KzGridsConsole`, `KzGridsPreview`, `KzGridsSlot`) and matching stub/template filenames stay as `KzGrids`. This is decoupled from the game-side identifiers (XML, deployed `.swf` filename, Aoc folder), which all rename to `KazBars`. Flash IDE recompilation of `base.fla` is needed to lift the freeze.
- **Project layout & tooling modernization (bundled with rebrand).** Source moved from `Modules/` to `src/kazbars/` (PEP 621 src layout). New `pyproject.toml` declares the project, deps, and tooling configs (`ruff`, `mypy`, `pytest`). New checked-in `kazbars.spec` replaces the dynamic PyInstaller invocation in `build.py`. New `.pre-commit-config.yaml` runs `ruff` + smoke tests. New `.github/workflows/ci.yml` runs lint + format + tests on push. Existing `release.yml` ported to `uv` + `pyinstaller kazbars.spec`. `kzgrids.py` split into `src/kazbars/__main__.py` (entry) + `src/kazbars/app.py` (`KazBarsApp` class) + `src/kazbars/__init__.py` (`__version__`, `APP_NAME`).
- **Settings migration.** `SettingsManager.__init__` now copies `settings/kzgrids_settings.json` → `settings/kazbars_settings.json` on first launch if the legacy file exists and the new one does not, so users keep their game folder, window position, and other preferences without reconfiguring.

## [Unreleased]

### Added
- Overlay padlock icon is now clickable to lock the overlay in place — no need to reach back into the app to lock it. Clicking the unlock glyph in the overlay toggles to the lock glyph and the app's Lock button updates to "Unlock" automatically. Unlock still has to come from the app, by design: locking enables OS-level click-through (`WS_EX_TRANSPARENT`) so game clicks pass through to AoC underneath, which means clicks on the lock glyph also pass through and can't trigger an unlock.
- `pywin32` declared as a real Windows-only dependency. Previously the click-through (the whole point of "Lock") was guarded by `try: import win32con` and silently did nothing in shipped builds because `pywin32` was never installed. With this release, locking the overlay actually lets you click through to the game UI behind it.
- Live Tracker overlay text is now rendered with an 8-direction outline stroke when transparent background is on, so phase messages and timers stay legible over Ethram-Fal's fire effects and other busy game scenes. The stroke layer toggles automatically with the Transparent background checkbox; off when the dark inner panel is showing (contrast there is already AAA on `#1a1a1a`).
- Live Tracker overlay rebuilt as a two-canvas docked layout. The cycle timer lives in a fixed-height bottom dock with the lock indicator and resize handle; message rows fill the remaining space above. The cycle timer can no longer overlap the messages on resize regardless of how short the user squishes the window. The overlay's minimum height now scales with font size via `_min_height()`, so the layout never collapses below the natural height of two readable rows + cycle timer + chrome; bumping font size also auto-grows the overlay if the new minimum exceeds the current height.
- Live Tracker profile load now restores the overlay's width, height, and visible state alongside position/opacity/transparent/lock/font. Previously `apply_settings` silently dropped width/height/visible, so a profile-saved overlay layout couldn't round-trip.
- Help section for the Ethram-Fal Live Tracker. Four short subsections matching the rest of the help's editorial register: Setup (`/logcombat on` + Start Monitoring + Test Cycle), Positioning (drag, ○/● lock glyph, click-through, where unlock lives), and The cycle (the ~40s rhythm: Viscous Seed → Lotus Fixation → silence → scorpion kill window 31s to next seed; plus Phase 4 double seed and Syphon clouds).

### Changed
- Ethram-Fal seed phase strings tightened: "Bring Scorp to the pile" stays up two seconds longer (15–18s instead of 15–16s), then the row1 message becomes "Kill window in Xs" (warning) for 19–27s before escalating to alert color at 28s. "Scorp" / "Scorps" renamed to "Scorpion" / "Scorpions" in every overlay message ("First Seed - Scorpion Soon", "Bring Scorpion to the pile", "Dps Scorpion to 5%", "Kill Scorpion!", "Kite Scorpions").
- Live Tracker enforces mutual exclusion between Test Cycle and Start Monitoring. Starting a test cycle disables the "Start Monitoring" button until the test ends (manually or after the natural ~40s reset); starting monitoring already disabled "Test Cycle". You can no longer wedge the two modes into running simultaneously.
- "Transparent background" tooltip rewritten — was misleadingly tied to click-through behavior, which is actually controlled by Lock state.
- Live Tracker overlay lock indicator changed from emoji glyphs to monochrome geometric circles (●/○) to match the typographic register of the resize-handle triangle (◢) per DESIGN.md's "tool-honest, not gamer-loud" principle. Color emoji shipped through Segoe UI Emoji and looked off-brand against the rest of the dark workbench.
- Live Tracker overlay now renders message rows and the cycle timer through `tk.Canvas` items instead of stacked `tk.Label` widgets. Required for the stroke layer; also removed the fixed `width=5` that clipped the timer column at large font sizes. Chrome (lock indicator, resize handle) stays as `tk.Label`s placed on the cycle-timer canvas.
- Settings file `timers_settings.json` renamed to `live_tracker_settings.json` to match the rebrand. One-shot rename via `_migrate_legacy_filename` in `live_tracker_settings.py`, mirroring the `kzgrids_settings.json` → `kazbars_settings.json` migration in `settings_manager.py`. Existing users keep their saved overlay state. Window-position settings key `window_pos_boss_timer` likewise migrated to `window_pos_live_tracker` on first launch via `_migrate_window_position_key` in `live_tracker_panel.py`.
- "Overlay opacity" slider tooltip rewritten ("30% = mostly transparent, 100% = solid"). The old "Overlay window transparency (30% to 100%)" was confusing because higher opacity reads as less transparent.
- Internal: Live Tracker cluster (`boss_timer.py`, `live_tracker_panel.py`, `timer_overlay.py`) hardened across three audit cycles (13/20 → 17/20 → 20/20). Defaults route through `TIMERS_DEFAULTS` everywhere via `.get(key, TIMERS_DEFAULTS[key])` (was: literal fallbacks that disagreed with the defaults dict). Cross-thread / cross-event-loop callbacks are named methods (`_dispatch_overlay_update`, `_apply_overlay_update`, `_run_game_tick`, `_test_trigger_fixation`, `_test_check_reset`, `_block_close`, `_on_lock_click`, `_arm_click_through_on`) instead of inline lambdas/closures. `update_display` short-circuits when state hasn't changed (at 50ms cadence the integer-second hasn't ticked ~9 of every 10 calls, so canvas churn drops by ~90%). Click-through (`WS_EX_TRANSPARENT`) now arms via `after_idle` instead of `after(100, ...)`, closing a brief startup race where the overlay was visible and locked but not yet click-through. `apply_settings` suspends per-setter notifications so a profile load saves once instead of four times. Lock state is now reached via an idempotent `set_locked(target)` setter; `toggle_lock` delegates. Cycle timer color switched from `THEME_COLORS['muted']` (editor palette) to `COLORS["default"]` (Tracker Idle), so the overlay surface uses zero editor-palette tokens. Live Tracker dialog header uses the new `MODULE_COLORS['live_tracker']` instead of `MODULE_COLORS['grids']`.
- Internal: Live Tracker cluster simplify pass — `boss_timer.py` 415 → 381, `timer_overlay.py` 589 → 542, `live_tracker_panel.py` 576 → 575. **Source-side dedupe.** `BossTimer.update_display` now caches `_last_phase` and short-circuits when the new phase dict equals the cached one, so the Tk `after(0, ...)` hop is skipped ~19 of every 20 ticks (was: dedupe only at the overlay leaf — the schedule still fired every tick). **Single-dict callback signature.** `BossTimer._update_callback` now passes one phase dict instead of nine kwargs (`row1_msg, row1_player, row1_timer, row1_color, row2_msg, ...`); `TimerOverlay.update_display(state)` accepts the same dict. Eliminates the dict→kwargs→dict round-trip in the hot path. **Per-canvas dirty redraws.** `TimerOverlay.update_display` now diffs per canvas — `_redraw_text_canvas()` only fires when a row* field changed, `_redraw_cycle_timer()` only when `cycle_timer` changed — so a 1/sec cycle-tick doesn't rebuild the message rows. **`_suspend_notify` retired.** `set_opacity`, `set_font_size`, `set_transparent`, `set_locked` all gained a `notify=True` keyword matching `show`/`hide`; `apply_settings` calls them with `notify=False` and `_notify_settings_changed()` once at the end (cleaner than the suspend-flag ping-pong, and the special `__init__` `_suspend_notify=True` ping-pong around the initial `set_locked` call also dropped — initial UNLOCK_GLYPH is set in `_build_ui` so `set_locked(False)` early-returns naturally). **State derived where free.** `BossTimer.seed_detected`/`fixation_detected` boolean fields removed — both were always set in lock-step with `seed_player`/`fixation_player`, so they're now derived as `*_player is not None` at use sites. **Reset extracted.** New `_reset_cycle_state()` (caller holds the lock) is shared by `start_cycle`, `stop_cycle`, and the cycle-completion branch of `get_current_phase` (was: the same six-field reset duplicated three times, including the inline-under-lock case that had to stay inline to avoid a re-entrancy window with `start_cycle`). **Smaller wins.** `_apply_background` lookup-dict + dropped silent `try/except (TclError, AttributeError)` swallowing (widgets are owned by `self`); `_block_close` empty method → `lambda: None`; `_arm_click_through_on` one-line wrapper → inline `after_idle(lambda: ...)`; `live_tracker_panel.py` parallel `_LOG_STATE_FG`/`_LOG_STATE_OVERLAY` dicts merged into a single tuple-valued `_LOG_STATE_STYLE`; `GAME_TICK_MS = 50` constant replaces two hardcoded `50` magic numbers in the game loop schedule. The named cross-thread dispatcher pair (`_dispatch_overlay_update` / `_apply_overlay_update`) survives the rewrite — only the payload changed from `**kwargs` to a single `phase` dict.

### Fixed
- After a syphon event followed by Stop Monitoring, the overlay would show "Avoid the clouds" indefinitely on the next Start Monitoring because `stop_cycle` didn't reset `syphon_active`. `stop_cycle` now clears all special-mechanic flags.
- `_set_click_through` no longer silently swallows pywin32 errors; failures are now logged.
- Live Tracker overlay was re-centering on the screen every launch since the rebrand. The `positioned` flag — which gates "have I been placed before?" — was being stripped from settings on every save: `validate_all_settings` keeps only keys that exist in `TIMERS_DEFAULTS`, and `positioned` was missing from that dict. Added `positioned: False` to `TIMERS_DEFAULTS` and registered it as a bool in `validate_setting`, so saved x/y now actually take effect on restore.
- About popup is now single-instance — repeat clicks on the menu's About button lift and focus the existing popup instead of stacking a new one each time. Pattern matches the boss-timer single-instance gate (`_open_boss_timer` / `_boss_timer_if_alive`): `_show_about` checks `getattr(self, '_about_popup', None)` and `winfo_exists()`, calls `deiconify/lift/focus_set` if alive, otherwise creates a fresh one and stores it. `show_about_popup` now returns the `Toplevel` so the caller can hold the reference. Audited every other clickable popup trigger in the app — all the other dialogs (`BuffDisplayDialog`, `BuffEditDialog`, `AddGridWizard`, `BuffSelectorDialog`, `SlotAssignmentDialog`, `show_close_game_required_dialog`) are `grab_set()` modals so their triggers are unreachable while alive; `LiveTrackerPanel` already had its own single-instance gate; `BuildLoadingScreen` is fronted by the `_building` re-entry flag in `build_action.build`. About was the only window that combined a frameless / non-modal style with a clickable affordance that stays live.

### Changed
- Source affordance colorized in both Add Grid surfaces. The Player and Target radio labels now carry their type accent (cyan `#3498db` / orange `#e67e22`, bold), the four preset shape buttons in the Add Grid wizard recolor live (`info-outline` ↔ `warning-outline`) when the source flips, and the empty-state preview cells + Custom card's `+` glyph track the same color via the existing `_redraw_empty_cards` path. Implementation note: ttkbootstrap's `tb.Window` strips unrecognised `style=` kwargs at widget construction (parses them as bootstyle keywords), so the two new `Player.TRadiobutton` / `Target.TRadiobutton` styles in `setup_custom_styles` have to be applied via `.configure(style=…)` after the widget exists. Empty-state row-height misalignment also fixed: the 10×1 vertical bar preset card was 200 px tall vs the 130 px squares so it stuck up above the row; dropped to `PRESET_CARD_SQUARE` (130) so the row tops align. Subtle: bumped the Source row's bottom padding from `PAD_XS` (4) to `PAD_LF` (8) so it doesn't crowd the Mode row beneath it.
- Top menu reorg. File menu gains **Exit** (after the separator, wired to `_on_close` so the unsaved-changes prompt still fires). Game menu gains **Change game folder...** and **Uninstall from game client...** (the latter moved out of File). The bottom-bar path-label context menu's "Change game folder…" was respelled to "Change game folder..." so the same command reads identically in both places. `CustomMenuBar` docstring trimmed of a stale "Alt activation" claim that was never implemented.
- Grids excluded from the build now visually mute in the editor list. Toggling Enabled off drains the grid's identity in lock-step: title drops from `heading` (`#FFFFFF`) to `dim_text` (`#888888`), the type badge drops to the same dim, the card frame border falls from the player/target accent to neutral `border` (`#444444`), and the internal accent strip (visible when expanded) follows. The shift is instant per DESIGN.md's *No-Decorative-Motion Rule*; the Enabled toggle stays the affordance, the fade is feedback. Tab focus into a disabled grid lights its title in `muted` rather than full `accent` so the row never looks live mid-tab. Internally: `bind_card_events` accepts a callable for its resting color so the same hover binding survives an enabled→disabled flip without rebinding; `CollapsibleSection` gained a public `set_dimmed()` and stores its accent canvas + content bar as instance state; `GridEditorPanel._apply_enabled_styling` is wired to the Enabled checkbox `command=` and re-applied on init.
- Toast system pass: coalesce, perf, voice, and consistency. **Coalesce by key** — `ToastManager.show` and `app_toast` now accept `key=`; live keyed toasts update in place instead of stacking. The static-grid resize trim/restore pair (and the `1×2` dynamic-coercion follow-up) all share `key='grid_resize'`, so holding the rows/cols spinbox arrow at full assignment produces a single in-place-updating toast instead of ~60 stacked ones over a 6 s burst. **Reposition perf** — hoisted `update_idletasks()` out of the inner `_reposition` loop (one trailing flush instead of one per toast); benefits every toast surface, not just the resize warning. **Entrance animation correctness** — `_animate_entrance` was reading `place_info()['y']` fresh each step and treating that as the target, but its own previous-step `place_configure(y=…)` had already overwritten that value, so the toast drifted progressively off-screen below; pre-coalesce this was masked by the per-tick `_reposition()` re-placing the toast every emit. Fixed by capturing `target_y` once before the step loop. **Brand fit** — toast bg switched from `Surface Sub` (`#1a1a1a`) to `Surface Input` (`#2f2f2f`) per DESIGN.md §5; `wraplength=360` on the label so long buff-name previews no longer clip on narrow windows; fade-out extended from 200 ms (8×25) to 420 ms (14×30) so the warning-yellow accent bar transitions smoothly instead of reading as a flash; resize-toast copy properly singularized ("Unassigned 1 buff" vs "Unassigned 3 buffs"). **API consistency** — every toast in the app now goes through `app_toast(widget, message, style, duration=, key=, on_click=)`; the seven direct `*.toast.show(...)` callers in `build_action.py`, `game_folder.py`, `profile_io.py`, `update_check.py`, and `database_editor.py` (13 sites) were converted, and `DatabaseEditorTab` lost its `toast=` constructor parameter and `self.toast` attribute since the walker pattern resolves the manager from any descendant. Default duration aligned (`ToastManager.show` 4 → 6, matching `app_toast`). `app_toast` gained `on_click=` passthrough for the update-check release-notes toast. **Pre-build auto-save no longer toasts** — `do_save_profile`/`_commit_saved_profile` accept `silent=True`; `build_action` passes it so the "Saved: …" toast doesn't pile up against the "Built — …" toast a few seconds later. **Build success toasts trimmed** — "Built — /reloadui in-game" / "Built — launch the game" / "Built — /reloadui + /reloadgrids" dropped the redundant "Built — " prefix; the success color and the build-loading screen carry the confirmation. **Onboarding step 4 tracks the deployed-SWF state** — the in-panel tip guide's Build step now resets to "not done" the moment any grid edit happens after a successful build (the deployed SWF no longer matches the in-memory state); previously it stayed green until the next build, which gave a misleading "you're done" signal after the user cleared all grids to start over. Step 4 also persists across app restarts: after a successful build, a SHA-1 signature of `{profile_path, grids}` is written to `settings/kazbars_settings.json` (`last_build_signature`); on profile load the same payload is recomputed and compared, so reopening the app on the same profile with no edits keeps step 4 green without requiring a rebuild. The profile path is part of the signature so cross-profile loads (e.g. building MyGrids then loading Default) correctly leave step 4 inactive even if the grids happen to hash to the same shape after migration; `None` is a valid identity, so building Default itself and later reloading Default also restores correctly.
- Internal: `profile_io.py` seam refactor. `load_profile` is split into `read_profile_file` (pure I/O, returns `(data, is_corrupt)`) and `apply_profile_data` (dispatch step — grids panel, missing-buff warning, **boss-timer fan-out when the live tracker is open**, reference_resolution, current_profile, settings, title). All four call sites — startup auto-load in `src/kazbars/app.py`, `open_profile`, `load_default_profile`, and the first-launch "Use Defaults" branch — now compose the two explicitly so the boss-timer dispatch is visible from every caller. The `_load_profile` delegator on `KazBarsApp` was removed. `do_save_profile` retained as orchestrator (its error-handling boilerplate would otherwise repeat at every save site); body factored into `build_profile_payload` (the live-tracker pull is named explicitly in its docstring), `write_profile_file`, and `_commit_saved_profile`. Resolves the architectural seam flagged in `docs/architecture-notes.md` Finding 1.
- Internal: Pylance type-checker pass. Two real bugs fixed: `GridEditorPanel.load_from_config` was passing `int` to `StringVar.set` for `x_var`/`y_var` (worked at runtime via tk's auto-coercion, now wrapped in `str()`); `DatabaseEditorTab.__init__`'s `toast=None` default was misleading because the sole caller always passes a `ToastManager` and 14 `self.toast.show(...)` sites assume non-None — `toast` is now a required keyword-only parameter. Remaining diagnostics are false positives from ttkbootstrap monkey-patching `bootstyle` onto `tkinter.ttk` widgets, suppressed inline with `# type: ignore[call-arg]` / `[call-overload]` on the offending lines in `src/kazbars/app.py`, `grids_panel.py`, and `database_editor.py`. Also suppressed `pack_forget` on a `winfo_children()` union narrowed to `Toplevel` — at runtime the children are only frames/widgets.
- Database editor pass: rows now tint by type (debuff in muted red `#F0A0A0`, misc in warm gold `#E0C880`), both AAA-contrast on the dark surface and paired with the Type column label so the encoding survives without color (DESIGN.md). Sortable column headings show ▲/▼ arrows on the active sort, with two trailing spaces reserved on inactive headings so the heading width stays constant when sorting flips. The empty-search state replaces the fake "No matches found" row injected into the tree with a centered `ttk.Label` overlay placed against the tree itself (so the scrollbar doesn't visibly offset it). Five blocking modal warnings (no-selection on edit/delete, empty import file, no-filter-match on export, "All" selected for rename, name-taken on rename, empty rename) are now toasts; a redundant `Messagebox.show_info` after the success toast on import/export was removed. Em dashes purged from user-facing strings (project-wide ban). The "Save Database" button width aligned to `BTN_MEDIUM`; Export tooltip honestly says "currently filtered" instead of "selected".
- Internal: `database_editor.py` cleanup pass — 904 → 866 lines. Centralized post-mutation refresh in `_after_db_change()` (5 callsites: add/edit/delete/import/rename-category). Unified the add/edit dialog validators into one `_make_buff_validator(old_ids=None, old_name=None)` (no args = add-mode, both args = edit-mode with row excluded from collision). Removed dead `BuffDatabase.find_entry_by_id` and the dead `state='normal'` resets in `_on_stacking_changed` (the partial toggle and `stack_start_spin` are never set to `'disabled'`). Hoisted the duplicated `result['stackStart']` write in `_apply_stacking_fields`. Renamed `parse_ids → _parse_ids` (single internal caller). Inlined `buff_type = TYPE_FILTER_MAP.get(...)` that was being shadowed by a per-buff loop variable in `refresh_list`/`export_buffs`. `create_widgets` does `buff = buff or {}` once, collapsing seven `buff.get(k, default) if buff else default` ternaries to plain `.get` calls. `migrate_legacy_buff_fields` no longer returns `buff` (in-place by design; only caller ignored the return). Tree-clear loop replaced with `tree.delete(*tree.get_children())`. Treeview empty-area background and the per-instance `_fix_tree_bg` `after_idle` shim moved into `ui_helpers.setup_custom_styles` so the global ttk style is configured once at app start instead of every tree build.
- Grids panel polish: `_create_from_empty_state` no longer crashes on the Custom preset card (typo fix). Save now guards against transient empty/`-` spinbox text — previously a Ctrl+S during a multi-key spinbox edit would raise `ValueError`. Empty-whitelist label updated to be actionable ("No buffs tracked. Click 'Tracked Buffs...' to add some."). Sort combobox widened so "Shortest first" no longer clips on Windows DPI scales. Empty-state preset cards now reflow into multiple rows on `<Configure>` when the wrapper is too narrow. Slot-counter danger prefix tightened (`!  ` → `! `). Unicode escapes in the panel summary line normalized to lowercase form for consistency with preset labels. No user-facing change in behavior beyond the bug fixes.
- Internal: `grids_panel.py` cleanup pass — dropped six dead `self.` writes (`_x_entry`, `_y_entry`, `_mode_btn`, `_rows_spin`, `_cols_spin`, `_info_row`/`_info_label`) that were assigned but never read; localized `_timer_group`/`_flash_group` to the building method; consolidated the parallel `_preset_cards` list into the existing `_empty_cards` (which already carried `card_w`); reused `bind_label_hover_colors` for the drag-handle Enter/Leave that was hand-rolled; folded `save_to_config`'s name-validation copy into a single `_validate_name()` call; dropped unused widget returns from `_add_str_spin`/`_add_position_entry`/`_add_spinbox`; trimmed verbose docstrings and decorative comment markers. `grids_panel.py`: 1218 → 1186 lines.
- Internal: extracted shared dialog button width and form-input width tokens. New `BTN_DIALOG = 10` (Cancel/Save/Done/Create pairs) and `INPUT_WIDTH_NUM/_TYPE/_FILTER/_SEARCH` (numeric spinboxes, compact dropdowns, filter combos, search entries) replace literal `width=10`/`width=5`/`width=18`/`width=20` repeated across `database_editor.py` and `grid_dialogs.py`. `BuffEditDialog` collapses its dual `_action_label` / "OK or Save" branching into a fixed "Save" button with a `_header_verb` for the dialog header text. Treeview empty area now matches the working surface (`fieldbackground=TK_COLORS['bg']`) so list panels don't show ttkbootstrap's lighter default below the last row.
- Window: minimum size lifted from 900×500 to 950×660 so the bottom bar's status indicators stay readable at the smallest geometry. The bottom bar now packs with `before=self.content_frame`, fixing the bug where vertical-resizing the window let the expand=True content frame eat the docked bar's space. New default geometry on first launch is also 950×660.
- Internal: shaved 128 lines from the four largest modules. `build_loading.py` (914 → 796): the four near-identical popups (`show_close_game_required_dialog`, `show_welcome_popup`, `show_about_popup`, `BuildLoadingScreen._build_summary_ui`) now share three module-level helpers — `_make_popup_shell` (Toplevel + border + scanlines + KAZ GRIDS title), `_draw_close_button` (rect + glow + hover hit-test + Escape/Return binds), `_center_popup`. `grids_panel.py` (1136 → 1128): collapsed `_add_pos_spin`/`_add_dim_spin` into one `_add_str_spin`, inlined the `_on_rows_changed`/`_on_cols_changed` 2-line passthroughs as lambdas, extracted `_attach_panel` for the duplicated separator+pack+drag-bind+reorder-bind sequence shared by `refresh_panels`/`_repack_panels`. `database_editor.py` (891 → 888): folded the 6-line show/hide branches in `_on_stacking_changed` through `getattr(w, method)`. `grid_dialogs.py` (742 → 743): added `_section()` helper for the `LabelFrame + padding + pack` triple in `AddGridWizard.create_widgets`. No user-facing changes.
- Internal: shrank `src/kazbars/app.py` from 741 to 621 lines. The first-launch flow's coordinator (closures + welcome-popup plumbing) moved into `src/kazbars/first_launch.py` as `run_first_launch(app, app_name)`. Resolution scaling moved out of the root window: `parse_resolution()` now lives in `grid_model.py` and `GridsPanel.scale_to_resolution()` owns the grid-coordinate math (no more reaching into `grids_panel.grids` from `src/kazbars/app.py`). Eight dead delegators (`_format_game_path`, `_prompt_aoc_bypass`, `_update_build_state`, `_pulse_game_hint`, `_save_game_path`, `_save_aoc_bypass`, `_warn_missing_buffs`, `_do_save_profile`) were removed once their last internal callers folded into satellites. Hoisted `threading`/`urllib` imports to module top; added `tk.TclError` to the update-checker's swallowed-exceptions tuple to silence shutdown-race tracebacks. Dropped three view-name aliases and a redundant `current_view` reset. No user-facing changes.
- Internal: split `src/kazbars/ui_helpers.py` (1,497 lines, 8 responsibilities) into six focused modules: `ui_helpers` (design tokens), `ui_widgets`, `ui_components`, `ui_tk_style`, `window_position`, `settings_manager`. Imports inside `src/kazbars/` are now consistently relative. No user-facing changes.
- Internal: shrank `src/kazbars/app.py` from 1,152 to 741 lines by extracting profile I/O → `src/kazbars/profile_io.py`, game-folder UI → `src/kazbars/game_folder.py`, Build & Install action → `src/kazbars/build_action.py`, and consolidating settings plumbing + JSON helpers into `src/kazbars/settings_manager.py`. `CustomMenuBar` moved out of `ui_components.py` into its own `src/kazbars/custom_menu_bar.py`. Pruned the defensive PyInstaller `--hidden-import` block in `build.py` (PyInstaller discovers all modules via static analysis). No user-facing changes.
- Internal: simplified the four largest functions in `grids_panel.py` and the five largest in `database_editor.py` by extracting builder methods. `GridEditorPanel.__init__` (232 → 60 lines) and `_build_empty_state` (124 → 58 lines) now delegate to focused `_build_*` helpers; `update_labels` reuses a new `_format_buff_preview` for the truncated-name preview that was duplicated for whitelist and slot-assignment paths. `BuffEditDialog.create_widgets`/`on_ok` and `DatabaseEditorTab.create_widgets`/`refresh_list`/`import_buffs` now use `_add_grid_row`, `_build_stacking_section`, `_collect_inputs`, `_apply_stacking_fields`, `_build_filter_bar`/`_build_tree`/`_build_button_bar`, plus module-level `format_stack_indicator` and `migrate_legacy_buff_fields`. New `bind_label_hover_colors` helper in `ui_widgets.py` centralizes the Enter/Leave/FocusIn/FocusOut foreground-toggle pattern previously copy-pasted on hover-able header labels. No user-facing changes.

### Added
- **Default Buff Bars editor** (Game → Default buff bars…). New modal that edits four AoC HUD XML files in place — Player + Target portraits (`CharPortraitLeft/Right.xml`), Top bar (`HUDView.xml`), Floating portraits (`FloatingPortraitView.xml`). One collapsible section per file with the same form: a "Show buff list" toggle that wraps the `<BuffListView />` element in a `<!--KZ_OFF ... KZ_OFF-->` comment span when off (game ignores it, the editor still sees it for re-enable), icon size + spacing spinboxes (8–128 px / 0–50 px, written as `Point(N,N)`), max-columns spinbox (1–30), and a Friendly / Hostile / Friendly + Hostile filter radio group. Writes go only to `Customized/`; the source under `Default/` is read-only. **Mirror-the-XML semantics:** the form blanks any field whose source attr is missing or unrecognised, and Apply only writes attrs the user explicitly set, so a stock file with no `filter` attribute stays clean if the user never picks one — and a custom UI's exotic `filter="all"` survives an icon-size change verbatim. **Surgical regex edits:** the `<BuffListView />` tag is the only thing rewritten; every byte outside it is preserved. Per-attr skip-when-equal keeps the file byte-identical when the user only touches one field. Missing attrs are injected before the closing `/>` (the previous "no-injection" rule meant the user's filter pick never reached stock files). One-shot `*.kazbars.bak` backup written next to each Customized file the first time the editor touches it. Section open/closed state persists across opens via `settings['buff_display_section_open']`; first-launch default opens Player only (others collapsed). Layout: scrollable body with the bottom Cancel | Apply row pinned (packs `side='bottom'` first so it can't be squeezed at any window size); Apply uses `success` bootstyle for primary affordance; success and failure both go through `app_toast` (failure with `key='buff_apply_failed'` so retries coalesce). Inline "(no filter set in source)" hint appears next to the radios when `filter_var` is empty so all-radios-unselected reads as intentional rather than broken. Custom-UI banner ("Edits stay in your skin.") only renders when `Customized/Views/HUD/` contains files we don't manage. The pre-flight no-game-folder check is the only modal in the module — toast can't render before the dialog exists.
- Smoke tests: `tests/test_imports.py` (full import-graph check), `tests/test_data_integrity.py` (buff-ref resolution in `Default.json` + bundled-DB sync check), and `tests/test_buff_xml.py` (round-trips the `buff_display_editor` regex helpers — attribute extraction, surgical replacement, KZ_OFF wrap toggle, filter normalisation, no-`<BuffListView>` guard). Run all three before every commit.

## [1.1.0] — 2026-04-22

### Added
- Background check for new releases on launch. Click the toast to open release notes.
- File → Uninstall from game client. Cleanly removes KazBars files from the selected install.

### Changed
- Grids now reference buffs by spell ID instead of name. Renaming a buff in the database no longer breaks grids that use it. Existing profiles migrate automatically on load; any buffs that can't be resolved are listed in a dialog so you can re-add them.
- About dialog now links to the GitHub repo.

### Fixed
- Database changes are now saved when you close the app and click "Yes" on the unsaved-changes prompt. Previously only the profile was saved.
- Build now refuses to run if an enabled grid has no tracked buffs, instead of installing a silent grid.
- Invalid entries in the Add Buff ID field show a warning before saving instead of being dropped silently.
- Welcome popup no longer references a non-existent "Edit → Clear All Grids" menu path.

## [1.0.0] — 2026-04-18

Initial public release.
